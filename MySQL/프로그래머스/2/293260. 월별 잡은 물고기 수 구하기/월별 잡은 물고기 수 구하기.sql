WITH MONTHLY_FISH AS (
    SELECT
        DATE_FORMAT(TIME, '%m') + 0 AS MONTH
    FROM
        FISH_INFO
)
SELECT
    COUNT(*) AS FISH_COUNT,
    MONTH
FROM
    MONTHLY_FISH
GROUP BY
    MONTH
ORDER BY
    MONTH
;